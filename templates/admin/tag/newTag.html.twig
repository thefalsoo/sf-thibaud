{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouveau Tags</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Créer</button>
        {{ form_end(form) }}
{% endblock %}
